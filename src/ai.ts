

type ChangeType = {
    changedFileName: string,
    changeLines: string,
    changeText: string,
    changeTime: string
}

export const aiChangeSummarizer = async({change}: {change:ChangeType}) => {

}